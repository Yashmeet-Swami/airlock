global:
  scrape_interval: 10s

scrape_configs:
  - job_name: airlock-gateway
    static_configs:
      - targets: ["gateway:3000"]
    metrics_path: /metrics

  - job_name: airlock-workers
    static_configs:
      - targets: ["workers:3001"]
    metrics_path: /metrics
